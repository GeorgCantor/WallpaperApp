<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_audi"
    app:startDestination="@id/audiFragment">

    <fragment
        android:id="@+id/audiFragment"
        android:name="com.georgcantor.wallpaperapp.ui.fragment.gallery.GalleryFragment"
        android:label="@string/audi"
        tools:layout="@layout/fragment_gallery" />

    <fragment
        android:id="@+id/favoritesFragment"
        android:name="com.georgcantor.wallpaperapp.ui.fragment.favorites.FavoritesFragment"
        android:label="@string/favorites"
        tools:layout="@layout/fragment_favorites" />

</navigation>